{
  "name": "my-counter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rimraf node_modules && rimraf dist && rimraf yarn.lock"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "@movefuns/move-js": "../../",
    "@wasmer/wasi": "1.2.2",
    "@wasmer/wasm-transformer": "^0.12.0",
    "@wasmer/wasmfs": "^0.12.0"
  }
}
